# ETT 油温予測 PoC — 変圧器の予防保全に向けた先読みの技術検証

電力用変圧器の油温（OT）を数時間先まで予測し、「高温になる前に知らせる」ことがどの程度できるかを、公開ベンチマーク ETT で検証した PoC です。
クライアント向けの報告スライドは `reports/slides/` に置きます（PDF）。

## 結論（テスト期間 2017-11 〜 2018-06）

| 設備 | 6h 先の MAE | 最強ベースライン比 | 持続予測比 | 高温の事前検知（学習期間 95% 点） |
|---|---|---|---|---|
| ETTh2 | **1.95 ℃** | −18%（リッジ 2.37） | −56%（4.43） | 13 イベント中 10 件を検知（77%）、通知 0.05 件/日、誤通知 0.01 件/日、平均 4.0 時間前 |
| ETTh1 | 1.11 ℃ | −4%（持続 1.15） | −4% | テスト期間に高温イベントなし → 評価保留 |

- 1h 先は直前値でほぼ当たる（ETTh1 0.42 ℃、ETTh2 0.37 ℃）。24h 先はどの手法も持続予測に勝てない（学習が 1 本目の木で止まる＝日内・季節の周期以外に手がかりがない）。
- 効いている情報は油温の履歴と**カレンダー（時刻・季節）**。負荷 6 種の追加価値は小さく、「対象時刻の負荷が既知」という条件（課題ルール）でも改善は 0〜2%。
- 予測できるのは油温と「相対的高温」の代理指標まで。故障・点検・介入の記録が無いので、保全効果そのものは今回は評価できない。

## 課題の設定と仮定

- **記法**: 起点 s = 予測を行う時刻、対象 T = s + h。目的変数は OT[T]。
- **2 つの評価条件**: 条件 B = 起点 s までの観測だけ（運用条件）。条件 A = B ＋ 対象時刻 T の負荷 6 種（課題ルール「t=T の油温を予測する際には t=T での特徴量を使用してよい」の解釈。将来の負荷が運転計画で既知という条件付き）。
- **分割（暦で固定）**: 学習 2016-07-01〜2017-06-30、検証 2017-07-01〜2017-10-31、テスト 2017-11-01〜2018-06-26。区切りは対象時刻 T で行い、学習ラベルが検証期間に食い込まない。設計・選択は検証期間だけで行ったが、EDA は全期間で行っているのでテストを「完全に未見」とは言わない。
- **主ホライズン 6h**（数時間前の確認・運転調整を想定）。1h・24h は補助。ホライズンごとに直接予測。
- **高温の代理指標**: 学習期間の OT 上位 5% 点（h1 35.2 ℃、h2 46.4 ℃）を固定。設備の危険温度ではない。上位 10% 点も併記。
- **警報の 2 種類**: 「高温状態の予測」（対象時刻に閾値超えかを当てる）と「高温になる前の検知（onset）」（起点では閾値以下、対象で超え。連続超過は 1 イベント、連続警報は 1 通知に集約）。
- 設備ごとに別モデル。運用時も OT は毎時観測できる前提（欠測時の代替推定は対象外）。到着遅延 0、再学習なし。
- 事前に置いた成功条件: 6h・条件 B で最強ベースラインより MAE 10% 以上改善、通知 1 件/日以下で onset 再現率 50% 以上 → ETTh2 は満たす、ETTh1 は精度改善が小さく警報は評価不能。

## 手法

- ベースライン: B0 持続予測 OT[s]、B1 季節ナイーブ OT[T−24h]、B2 リッジ回帰。
- 本命: LightGBM（L1 目的関数、検証期間で早期終了）。目的変数は残差 OT[T]−OT[s]（検証で直接予測より良かった）。特徴量は OT の履歴（現在値・代表ラグ・差分・移動統計）、対象時刻の周期特徴、負荷の現在値・差分・24h 平均、（条件 A のみ）対象時刻の負荷。
- リーク防止: 条件 B の特徴量は末尾を切っても値が変わらないことを `features.check_no_leakage` で確認。スケーラーは学習期間で fit。
- アブレーション（6h）: OT 履歴のみ → ＋カレンダー → ＋起点までの負荷 → ＋対象時刻の負荷。

## 再現手順

```bash
uv sync
uv run python scripts/run_eda.py        # reports/figures/eda_*.png, reports/eda_summary.md
uv run python scripts/run_baselines.py  # reports/results_baselines.md
uv run python scripts/run_lgbm.py       # reports/results_lgbm.md（約 1.5 分）
uv run python scripts/make_figures.py   # reports/figures/res_*.png
```

Python 3.12 / pandas / scikit-learn / LightGBM 4.7。乱数シードは固定。

## リポジトリ構成

```
data/            ETTh1, ETTh2, ETTm1, ETTm2（公開データ。出典: zhouhaoyi/ETDataset）
src/ett_poc/     data.py（読込・分割）, features.py, evaluate.py, models.py
scripts/         run_eda.py, run_baselines.py, run_lgbm.py, make_figures.py
reports/         数表（自動生成）・図・スライド
```

## 限界と次のステップ

- 24h 先は現状の情報では予測できない。外気温・気象予報の追加が最有力。
- 2 台で良好でも未学習の設備への汎化は示していない。設備ごとの再学習と、ドリフトの監視が要る。
- 高温イベントが少ない期間（h1 のテスト期間）では警報性能を評価できない。長期のデータか、複数設備の横断評価が要る。
- 現場実証に進む条件: 対象設備の OT と負荷が毎時取れること、閾値の業務上の意味（危険温度・運転上限）の確定、警報を受けて誰が何をするかの運用設計。

## 出典

- Zhou, H. et al. "Informer: Beyond Efficient Transformer for Long Sequence Time-Series Forecasting" (AAAI 2021). データ: https://github.com/zhouhaoyi/ETDataset
- LightGBM: Ke, G. et al. (NeurIPS 2017)
